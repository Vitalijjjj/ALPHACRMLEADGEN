import LeadsView from "@/components/leads/LeadsView";

// Potential: ліди на гарячих стадіях (КП, Інвойс, дотики, Думає, Цікаво) + всі із запланованим пушем
export default function PotentialPage() {
  return <LeadsView potential />;
}
